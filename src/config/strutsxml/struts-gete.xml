<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
	<package name="globalpackage" extends="struts-default">
         	<!-- 定义全部的拦截器 -->
    	<interceptors>
    		<!-- 定义第一个自定义的拦截器，用于身份校验 -->
    		<interceptor name="isloginInterceptor" class="com.gete.framework.interceptor.IsLoginInterceptor" />
			<!-- 定义一个拦截器栈，系统所有Action都需配置 -->
			<interceptor-stack name="islogin">
				<interceptor-ref name="isloginInterceptor"></interceptor-ref>
				<interceptor-ref name="defaultStack"></interceptor-ref>
			</interceptor-stack>
    	</interceptors>
    	
		<default-interceptor-ref name="islogin"/>
		
		
		
    	<global-results>
    		
			<result name="login">/error/loginerror.jsp</result>
			<!-- 下面定义的结果对所有的Action都有效 -->
	    	<result name="exception">/error/error.jsp</result>
		</global-results>
		
		<global-exception-mappings>
			<!-- 指Action抛出AuctionException异常时，转入名为exception的结果。 -->
			<exception-mapping exception="com.gete.framework.exception.GlobalException" result="exception"/>
			<!-- 指Action抛出Exception异常时，转入名为exception的结果。 -->
			<exception-mapping exception="java.lang.Exception" result="exception"/>
		</global-exception-mappings>    
	</package>
</struts>